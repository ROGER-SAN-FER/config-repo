server:
  port: 8082  # Puerto exclusivo para este microservicio

spring:
  jpa:
    hibernate:
      ddl-auto: update   # en prod: validate o usa Flyway/Liquibase
    properties:
      hibernate:
        format_sql: true
    open-in-view: false